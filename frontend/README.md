# Frontend

Planned React/HTML client for product browsing, cart interactions, and checkout.

Phase 1 includes only the repository structure and minimal package metadata. Application code, dependencies, build tooling, and tests are deferred to later phases.
